<UserControl x:Class="APPD.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:APPD"
             xmlns:views="clr-namespace:APPD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="675" d:DesignWidth="1200">
    <StackPanel Orientation="Vertical" Width="1200" Height="675">
        <views:NavBarView Panel.ZIndex="1"/>
        
        <WrapPanel Orientation="Horizontal" Width="2400" Height="595">
            <WrapPanel.RenderTransform>
                <TransformGroup>
                    <TranslateTransform x:Name="panelTransform" />
                </TransformGroup>
            </WrapPanel.RenderTransform>
            <StackPanel
                Orientation="Horizontal"
                Width="1200" Height="595">
                <views:TrendingView Panel.ZIndex="0" HorizontalAlignment="Left" VerticalAlignment="Top" />
                <views:HomeDisplayListView />
            </StackPanel>
        </WrapPanel>
    </StackPanel>
</UserControl>
